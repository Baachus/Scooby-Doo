using ScoobWebApp;
using ScoobWebApp.Producer;

namespace ScoobWebApp.Producer;

public class RelationshipUtil_Tests
{
    //[Fact]  TODO: Fails
    //public async Task CreateRelationship_ShouldReturnTheCreatedRelation()
    //{
    //    // Arrange
    //    var relation = new ScoobRelation { Id = 1, Name = "Test", Appearance = "test apperance", Gang=GangMember.Scooby, Relationship="Uncle"};
    //    var relationshipUtil = new RelationshipUtil();

    //    // Act
    //    var createdRelation = await relationshipUtil.CreateRelationship(relation);

    //    // Assert
    //    createdRelation.Should().NotBeNull();
    //    createdRelation.Id.Should().BeGreaterThan(0);
    //    createdRelation.Name.Should().Be(relation.Name);
    //    createdRelation.Appearance.Should().Be(relation.Appearance);
    //    createdRelation.Gang.Should().Be(relation.Gang);
    //    createdRelation.Relationship.Should().Be(relation.Relationship);
    //}
}
